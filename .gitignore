# NEEDLE execution traces are local diagnostics.
**/.beads/traces/

# NEEDLE dispatch bookkeeping: HEAD SHA at worker dispatch time, local-only.
/.needle-predispatch-sha

# Python bytecode from running tools/*.py
__pycache__/
